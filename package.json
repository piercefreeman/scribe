{
  "name": "website-builder",
  "version": "1.0.0",
  "description": "Personal blogging build system. FastAPI and flask is overkill - create a statically linked site.",
  "main": "tailwind.config.js",
  "dependencies": {
    "@tailwindcss/forms": "^0.5.3",
    "@tailwindcss/typography": "^0.5.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  },
  "scripts": {
    "styles-build": "npx @tailwindcss/cli -i ./style.css -o scribe/resources/style.css",
    "styles-watch": "npx @tailwindcss/cli -i ./style.css -o scribe/resources/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
